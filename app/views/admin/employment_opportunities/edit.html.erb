<h1>Editing employment opportunity</h1>

<% form_for :employment_opportunity, @employment_opportunity, :url => { :action => "create" } do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', admin_employment_opportunity_path(@employment_opportunity) %> |
<%= link_to 'Back', admin_employment_opportunities_path %>