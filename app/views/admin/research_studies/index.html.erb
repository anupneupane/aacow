<h1>Listing research studies</h1>

<table>
  <tr>
  </tr>

<% @research_studies.each do |opportunity| %>
  <tr>
    <td><%= link_to 'Show', opportunity %></td>
    <td><%= link_to 'Edit', edit_admin_research_study_path(opportunity) %></td>
    <td><%= link_to 'Destroy', opportunity, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New research study', new_admin_research_study_path %>